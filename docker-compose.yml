version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/opt/web2py/applications/eden
    restart: always
    environment:
      - WEB2PY_PASSWORD=password
